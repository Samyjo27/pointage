(function(){
  // Placeholder content script to ensure MV3 loads the environment across browsers
  // It can be expanded later to listen for additional messages
  window.addEventListener('message', function(event){
    if (!event || !event.data) return;
    // Reserved for future communication
  });
})();
